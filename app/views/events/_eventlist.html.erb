<h2>My Sess:</h2>

<%= link_to '<button type="button">Host</button>'.html_safe, new_event_path %>
<%= link_to '<button type="button">Attend</button>'.html_safe, events_path %>
<br>

<% @events.each do |event| %>
  	<br>
	  	<h4>Date:</h4><%= event.date.strftime("%A, %B %d, %Y") %>
	    <h4>Time:</h4><%= event.time.strftime("%l:%M %P") %>
	    <h4>Address:</h4><%= event.address %>
	    <h4>Host:</h4><%= event.host %>
  	<br>
	<br>

  <%= link_to '<button type="button">Remove</button>'.html_safe, root_path %>

  <br>

<% end %>